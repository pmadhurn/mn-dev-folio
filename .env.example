# Chat backend base URL. Leave unset in dev — the Vite proxy forwards /api to
# the local FastAPI backend (see vite.config.ts), which avoids CORS entirely.
# Only set this to point the frontend at a deployed backend, e.g.:
# VITE_CHAT_API_BASE_URL=https://chat-api.madhur.dev
